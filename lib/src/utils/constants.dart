import 'package:flutter/material.dart';

/// Default animation duration for loading indicators.
const defaultAnimationDuration = Duration(milliseconds: 1200);

/// Default size for loading indicators.
const defaultIndicatorSize = 40.0;

/// Default color for loading indicators.
const defaultIndicatorColor = Color(0xFF2196F3); // Material Blue

/// Default stroke width for circular indicators.
const defaultStrokeWidth = 3.0; 